# Speech Recognition
# NOTE: pyaudio requires system dependency: portaudio
#   Linux:   sudo apt install portaudio19-dev
#   macOS:   brew install portaudio
#   Windows: pre-compiled wheel ships with pip
azure-cognitiveservices-speech==1.34.0
pyaudio==0.2.14  # For microphone recording (Whisper/Google/Vosk STT)
SpeechRecognition==3.10.0  # For Google STT
vosk==0.3.45  # For offline STT

# Wake Word Detection
# Primary (free, offline, no API key):
openwakeword>=0.6.0
# Fallback (requires PORCUPINE_ACCESS_KEY + .ppn files):
pvporcupine==2.1.0

# Text-to-Speech
pyttsx3==2.90

# LLM
openai>=1.12.0

# Database (sqlite3 is built-in)

# Scheduling
APScheduler==3.10.4

# System Tray
pystray==0.19.5
Pillow==10.1.0
plyer>=2.1.0  # Cross-platform OS desktop notifications

# Media Control
pyautogui==0.9.54
pynput>=1.7.6  # Cross-platform keyboard hotkeys (Ctrl+Space activation)
# Windows-only: exact volume control via COM interface
pycaw; sys_platform=="win32"
comtypes; sys_platform=="win32"

# Configuration
PyYAML==6.0.1
python-dotenv==1.0.0

# Utilities
python-dateutil==2.8.2
psutil==5.9.8  # System information
requests==2.31.0  # HTTP requests for weather API

# Retry logic
tenacity==8.2.3
simpleeval==0.9.13
pint==0.23

# Google Integration
google-api-python-client==2.118.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# Embedded WebView Overlay
pywebview==6.1
# Linux webview backends:
#   GTK  (preferred, no xcb issues):
#     sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0 gir1.2-webkit2-4.0
#   Qt   (fallback, installed via pip):
qtpy>=2.4.1; sys_platform=="linux"
PyQt6>=6.7.1; sys_platform=="linux"
PyQt6-WebEngine>=6.7.0; sys_platform=="linux"
# Linux also needs libportaudio for PyAudio:
#   sudo apt install portaudio19-dev
# Linux TTS (espeak backend for pyttsx3):
#   sudo apt install espeak espeak-ng libespeak-ng-dev

# Local API server for desktop overlay state sync
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Build Tools
pyinstaller>=5.13.2
